spring:
  datasource:
    url: jdbc:mysql://localhost:3307/project_si_db?useSSL=false&serverTimezone=UTC
    username: project_user
    password: project_password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
